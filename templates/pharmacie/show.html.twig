{% extends 'base.html.twig' %}

{% block title %}Pharmacie{% endblock %}

{% block body %}
    <h1>Pharmacie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pharmacie.id }}</td>
            </tr>
            <tr>
                <th>Specialite</th>
                <td>{{ pharmacie.specialite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pharmacie_index') }}">back to list</a>

    <a href="{{ path('app_pharmacie_edit', {'id': pharmacie.id}) }}">edit</a>

    {{ include('pharmacie/_delete_form.html.twig') }}
{% endblock %}
