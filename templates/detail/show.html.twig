{% extends 'base.html.twig' %}

{% block title %}Detail{% endblock %}

{% block body %}
    <h1>Detail</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ detail.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ detail.quantite }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ detail.prix }}</td>
            </tr>
            <tr>
                <th>Total</th>
                <td>{{ detail.total }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_detail_index') }}">back to list</a>

    <a href="{{ path('app_detail_edit', {'id': detail.id}) }}">edit</a>

    {{ include('detail/_delete_form.html.twig') }}
{% endblock %}
